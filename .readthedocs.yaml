version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

# Enable automatic builds on GitHub push to main
on_version: main
